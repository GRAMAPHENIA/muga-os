---
import Layout from '../layouts/Layout.astro';

const notionApiKey = import.meta.env.NOTION_API_KEY;
const databaseId = import.meta.env.NOTION_BLOG_DB;
---

<Layout title="Debug - Muga OS" description="Diagnóstico del sistema Notion">
  <!-- Header -->
  <section class="px-6 py-20">
    <div class="text-center">
      <h1 class="text-3xl md:text-4xl font-bold text-white mb-6 uppercase tracking-wide">
        Sistema de Diagnóstico
      </h1>
      <p class="text-lg text-neutral-300 max-w-2xl mx-auto leading-relaxed">
        Verificación del estado de conexión con Notion y configuración del sistema.
      </p>
    </div>
  </section>

  <div class="max-w-3xl mx-auto px-6 py-16">
    <!-- Estado del Sistema -->
    <section class="mb-16">
      <h2 class="text-2xl font-bold text-white mb-8 uppercase tracking-wide">Estado del Sistema</h2>
      
      <div class="space-y-6">
        <div class="p-6 border border-neutral-800/60 hover:bg-neutral-900 transition-colors">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-lg font-semibold text-white uppercase tracking-wide">API Key</h3>
            <span class={`px-3 py-1 text-xs font-medium uppercase tracking-wide ${
              notionApiKey ? 'bg-green-900/50 text-green-300 border border-green-700' : 'bg-red-900/50 text-red-300 border border-red-700'
            }`}>
              {notionApiKey ? '✓ Configurada' : '✗ Faltante'}
            </span>
          </div>
          <p class="text-neutral-400 text-sm">
            Token de autenticación para acceder a la API de Notion
          </p>
        </div>

        <div class="p-6 border border-neutral-800/60 hover:bg-neutral-900 transition-colors">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-lg font-semibold text-white uppercase tracking-wide">Base de Datos</h3>
            <span class={`px-3 py-1 text-xs font-medium uppercase tracking-wide ${
              databaseId ? 'bg-green-900/50 text-green-300 border border-green-700' : 'bg-red-900/50 text-red-300 border border-red-700'
            }`}>
              {databaseId ? '✓ Configurada' : '✗ Faltante'}
            </span>
          </div>
          <p class="text-neutral-400 text-sm mb-3">
            Identificador de la base de datos de Notion para el blog
          </p>
          {databaseId && (
            <div class="bg-neutral-900/40 p-3 border border-neutral-800/60">
              <code class="text-neutral-300 text-xs break-all">{databaseId}</code>
            </div>
          )}
        </div>
      </div>
    </section>

    <!-- Configuración Rápida -->
    <section class="mb-16">
      <h2 class="text-2xl font-bold text-white mb-8 uppercase tracking-wide">Configuración Rápida</h2>
      
      <div class="space-y-6">
        <div class="p-6 border border-neutral-800/60">
          <div class="flex items-start gap-4">
            <span class="bg-neutral-800 text-white w-8 h-8 flex items-center justify-center text-sm font-bold">1</span>
            <div class="flex-1">
              <h3 class="text-white font-semibold mb-2 uppercase tracking-wide">Crear Integración</h3>
              <p class="text-neutral-300 text-sm mb-3">
                Ve a Notion Developers y crea una nueva integración interna
              </p>
              <a 
                href="https://www.notion.so/my-integrations" 
                target="_blank"
                class="inline-block bg-white text-black px-4 py-2 hover:bg-neutral-200 transition-colors text-xs uppercase tracking-wide"
              >
                Abrir Notion Developers
              </a>
            </div>
          </div>
        </div>

        <div class="p-6 border border-neutral-800/60">
          <div class="flex items-start gap-4">
            <span class="bg-neutral-800 text-white w-8 h-8 flex items-center justify-center text-sm font-bold">2</span>
            <div class="flex-1">
              <h3 class="text-white font-semibold mb-2 uppercase tracking-wide">Configurar Base de Datos</h3>
              <p class="text-neutral-300 text-sm mb-3">
                Crea una base de datos en Notion con estas propiedades exactas:
              </p>
              <div class="bg-neutral-900/40 p-4 border border-neutral-800/60">
                <ul class="space-y-1 text-xs text-neutral-400">
                  <li>• <span class="text-neutral-300">Titulo</span> (Título/Title)</li>
                  <li>• <span class="text-neutral-300">Fecha</span> (Fecha/Date)</li>
                  <li>• <span class="text-neutral-300">Tags</span> (Multi-select)</li>
                  <li>• <span class="text-neutral-300">Slug</span> (Texto/Text)</li>
                  <li>• <span class="text-neutral-300">Descripcion</span> (Texto/Text)</li>
                </ul>
              </div>
            </div>
          </div>
        </div>

        <div class="p-6 border border-neutral-800/60">
          <div class="flex items-start gap-4">
            <span class="bg-neutral-800 text-white w-8 h-8 flex items-center justify-center text-sm font-bold">3</span>
            <div class="flex-1">
              <h3 class="text-white font-semibold mb-2 uppercase tracking-wide">Compartir Acceso</h3>
              <p class="text-neutral-300 text-sm">
                En tu base de datos: Menú (⋯) → "Conectar a" → Selecciona tu integración
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Error Común -->
    {(!notionApiKey || !databaseId) && (
      <section class="mb-16">
        <div class="diagonal-stripes bg-neutral-900/40 p-8 border border-neutral-800/60">
          <h2 class="text-xl font-semibold text-white mb-4 uppercase tracking-wide">Error de Configuración</h2>
          <p class="text-neutral-300 mb-3 text-sm">
            <strong>"Database does not contain any data sources accessible by this API bot"</strong>
          </p>
          <p class="text-neutral-400 text-sm">
            Este error indica que la base de datos no está compartida con tu integración. 
            Completa el paso 3 de la configuración.
          </p>
        </div>
      </section>
    )}

    <!-- Navegación -->
    <section>
      <div class="text-center">
        <a 
          href="/blog" 
          class="inline-block bg-white text-black px-8 py-3 hover:bg-neutral-200 transition-colors font-medium text-sm uppercase tracking-wide"
        >
          Volver al Blog
        </a>
      </div>
    </section>
  </div>
</Layout>

<style>
  .diagonal-stripes {
    background-image: repeating-linear-gradient(
      -45deg,
      transparent,
      transparent 1px,
      rgba(115, 115, 115, 0.1) 3px,
      rgba(115, 115, 115, 0.1) 10px
    );
  }
</style>